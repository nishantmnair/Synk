# Frontend environment variables
# Copy to .env or .env.local and set your values. Vite only exposes vars prefixed with VITE_.
# AI (Plan Date, Pro Tip, Daily Prompt) uses the backend; set GEMINI_API_KEY in backend .env.

# Backend API base URL (optional; default: http://localhost:8000)
# VITE_API_URL=http://localhost:8000
